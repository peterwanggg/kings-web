
import { LatLon, INITIAL_STATE } from '../types/index';


// TODO: fix action
export const latLon = (state: LatLon = INITIAL_STATE.latLon, action: any) => {
    switch (action.type) {
        default:
            return state
    }
}

export const categoryId = (state: number = INITIAL_STATE.categoryId, action: any) => {
    switch (action.type) {
        default:
            return state
    }
}

